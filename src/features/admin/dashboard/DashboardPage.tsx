import{FolderTree,MessageCircle,Package,TrendingUp}from"lucide-react";import{useProducts}from"../products/hooks/useProducts";import{useCategories}from"../categories/hooks/useCategories";import{useInquiries}from"../inquiries/hooks/useInquiries";
export default function DashboardPage(){const{data:products=[]}=useProducts();const{data:categories=[]}=useCategories();const{data:inquiries=[]}=useInquiries();const newCount=inquiries.filter(x=>x.status==="new").length;const completed=inquiries.filter(x=>x.status==="completed").length;const stats=[{label:"محصولات",value:products.length,icon:Package,color:"bg-red-50 text-red-500"},{label:"دسته‌بندی‌ها",value:categories.length,icon:FolderTree,color:"bg-sky-50 text-sky-600"},{label:"استعلام جدید",value:newCount,icon:MessageCircle,color:"bg-amber-50 text-amber-600"},{label:"استعلام نهایی",value:completed,icon:TrendingUp,color:"bg-emerald-50 text-emerald-600"}];return <div className="space-y-6"><div><h1 className="text-2xl font-black sm:text-3xl">داشبورد مدیریت</h1><p className="mt-2 text-sm text-slate-500">نمای کلی وضعیت فروشگاه و درخواست‌های مشتریان</p></div><div className="grid grid-cols-2 gap-3 lg:grid-cols-4 lg:gap-5">{stats.map(s=>{const Icon=s.icon;return <article key={s.label}className="rounded-2xl border border-white/80 bg-white/90 p-4 shadow-sm sm:p-5"><span className={`grid h-11 w-11 place-items-center rounded-xl ${s.color}`}><Icon size={21}/></span><strong className="mt-5 block text-3xl font-black">{s.value.toLocaleString("fa-IR")}</strong><span className="mt-1 block text-xs text-slate-500 sm:text-sm">{s.label}</span></article>})}</div><div className="grid gap-5 xl:grid-cols-2"><section className="rounded-2xl border bg-white/90 p-5"><div className="flex items-center justify-between"><h2 className="text-lg font-black">آخرین استعلام‌ها</h2><span className="rounded-full bg-red-50 px-3 py-1 text-xs font-bold text-red-500">{newCount} جدید</span></div><div className="mt-4 divide-y">{inquiries.slice(0,5).map(i=><div key={i.id}className="flex items-center justify-between gap-3 py-3"><div className="min-w-0"><p className="truncate font-bold">{i.name}</p><p className="truncate text-xs text-slate-400">{i.productTitle}</p></div><a href={`tel:${i.mobile}`}dir="ltr"className="shrink-0 text-sm text-red-500">{i.mobile}</a></div>)}{!inquiries.length&&<p className="py-8 text-center text-slate-400">هنوز استعلامی ثبت نشده است.</p>}</div></section><section className="rounded-2xl border bg-slate-950 p-5 text-white"><h2 className="text-lg font-black">نرخ تبدیل استعلام</h2><p className="mt-2 text-sm text-slate-400">درصد استعلام‌هایی که نهایی شده‌اند</p><div className="mt-8 flex items-end gap-4"><strong className="text-5xl font-black text-red-500">{inquiries.length?Math.round(completed/inquiries.length*100):0}٪</strong><span className="pb-2 text-sm text-slate-400">از کل درخواست‌ها</span></div><div className="mt-6 h-3 overflow-hidden rounded-full bg-slate-800"><div className="h-full rounded-full bg-gradient-to-l from-red-500 to-orange-400 transition-all"style={{width:`${inquiries.length?completed/inquiries.length*100:0}%`}}/></div></section></div></div>}
